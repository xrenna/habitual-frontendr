import React, { Component } from 'react'

export default class Goals extends Component {
    render() {
        return (
            <>
            {this.props.name}
            </>
        )
    }
}
